name: "🚀 release"

on:
  push:
    branches:
      - main

jobs:
  release:
    name: 🚀 release
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 publish
        run: |
          cd ${{ github.workspace }}
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
